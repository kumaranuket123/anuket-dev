name: portfolio_backend
description: Dart Shelf HTTP server with MCP tools for Anuket's portfolio AI chat
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  dart_mcp: ^0.5.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  http: ^1.2.0
  stream_channel: ^2.1.0
